docker build -t .  (build the image)
docker images  (show the images)
docker image rm (remove the image)
docker ps  (show the running container)
docker ps -a (show the all running or stop container)
docker inspect [container name]  (show the all information of container)
ip a s (show the all ip address)
hostname -i (show inside the container ip address)
docker network create [nw_name] --driver bridge (create your own bridgr network)
docker network connect [nw_name] [container_name] (attached new network to container)
sudo usermod -aG ubuntu docker (permission to docker root user)
docker volume create [volume_name] (create a new volume)
docker-compose -f [yml filename] up -d (run the specific yml file)
